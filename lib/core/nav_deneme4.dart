import 'package:flutter/material.dart';

class Deneme4 extends StatelessWidget {
  const Deneme4({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
